This is a placeholder document for the LLM grounding challenge.
The RAG pipeline will chunk this text, embed it, and store it in ChromaDB.
On challenge day, replace this with the real dataset.

Key facts about this system:
- It uses ChromaDB as a local vector store.
- Embeddings are generated via the sentence-transformers library.
- The LLM is served through OpenRouter using google/gemini-2.0-flash-001.
- The Flask app exposes a /chat endpoint for querying.
- The ingest.py script processes and indexes all files in the /data directory.

To test the pipeline with this placeholder:
- Ask: "What vector store does this system use?"
- Ask: "What model is used for generation?"
- Ask: "How do I add new data?"
