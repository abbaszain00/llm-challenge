services:
  app:
    build: .
    ports:
      - "5000:5000"
    env_file:
      - .env
    volumes:
      # Persist ChromaDB across container restarts
      - ./chroma_db:/app/chroma_db
      # Mount data directory so you can swap files without rebuilding
      - ./data:/app/data
    restart: unless-stopped
